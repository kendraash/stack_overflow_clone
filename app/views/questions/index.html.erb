<h1>Questions</h1>
<ul>
  <% @questions.each do |question| %>
    <li><%= link_to question.title, question_path(question) %> by <%= question.user.username %></li>
  <% end %>
</ul>


<%= link_to "Ask Question", new_question_path %>
